apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.19.0 (f63a961c)
  creationTimestamp: null
  labels:
    app: {{ template "spring-petclinic.name" . }}-hystrix-dashboard
    chart: {{ template "spring-petclinic.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
  name: {{ template "spring-petclinic.name" . }}-hystrix-dashboard
spec:
  ports:
  - name: {{ template "spring-petclinic.name" . }}-hystrix-dashboard
    port: 7979
    targetPort: {{ template "spring-petclinic.name" . }}-hystrix-dashboard
  selector:
    app: {{ template "spring-petclinic.name" . }}-hystrix-dashboard
status:
  loadBalancer: {}
