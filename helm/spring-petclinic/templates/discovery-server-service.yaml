apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.19.0 (f63a961c)
  creationTimestamp: null
  labels:
    app: {{ template "spring-petclinic.name" . }}-discovery-server
    chart: {{ template "spring-petclinic.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
  name: {{ template "spring-petclinic.name" . }}-discovery-server
spec:
  ports:
  - name: {{ template "spring-petclinic.name" . }}-discovery-server
    port: 8761
    targetPort: {{ template "spring-petclinic.name" . }}-discovery-server
  selector:
    app: {{ template "spring-petclinic.name" . }}-discovery-server
status:
  loadBalancer: {}
