apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.19.0 (f63a961c)
  creationTimestamp: null
  labels:
    app: {{ template "spring-petclinic.name" . }}-config-server
    chart: {{ template "spring-petclinic.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
  name: {{ template "spring-petclinic.name" . }}-config-server
spec:
  ports:
  - name: {{ template "spring-petclinic.name" . }}-config-server
    port: 8888
    targetPort: {{ template "spring-petclinic.name" . }}-config-server
  selector:
    app: {{ template "spring-petclinic.name" . }}-config-server
status:
  loadBalancer: {}
